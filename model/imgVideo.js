class imgVideo {
  constructor(id, description, time, teacher, url) {
    this.id = id
    this.description = description
    this.time = time
    this.teacher = teacher
    this.url = url
  }
}

export default imgVideo
