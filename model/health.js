class Health {
  constructor(id, status, description, time, teacher) {
    this.id = id
    this.status = status
    this.description = description
    this.time = time
    this.teacher = teacher
  }
}

export default Health